from test_suite_io import *
from ctypes import *
#numpy already imported from test_suite_io as np

#write point files which shall remain same for each libraries test
#write config file for each inp file and each corresponding library inp[num].config_[lib_name]

#load library
#Each library is a separate class


#run only library
#read result points
#read other data in output file #which reflects experiment results
#run library with ltrace LATER
#get the function call count and store in a file LATER
